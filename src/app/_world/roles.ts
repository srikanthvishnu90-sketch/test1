/**
 * Demo identities that are not the teacher/student worlds. Plain constants (NOT a
 * "use server" module, which may only export async functions), so server
 * components and server actions can both import them.
 */

/** The demo counselor — the designated crisis recipient (P16). */
export const COUNSELOR_ID = "counselor-1";
export const COUNSELOR_NAME = "Mr. Okafor";
